# Create a new post every 10 minutes
*/10 * * * * root cd {{dev_directory}}/axel-hello && git pull && ./script dev

# Commit to staging every hour
0 * * * * root cd {{dev_directory}}/axel-hello && git pull && ./script staging
