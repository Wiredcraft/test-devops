不习惯用doc，下面是整体思路

按照题意，解析整体解决方案
1. 通过文件流读取文件内容，两行为一组，第一行内容以replaced开头，第二行以actionId开头
2. 异步处理每组数据，是否符合要求对象是否正确完整，不正确将id记录到数组中
3. 使用锁保证任务全部完成后进行写文件
4. 按照值的大小进行排序后写入文件

对于标准答案有两点异议
1. 题目中说明用户的Id为int类型，但是排序确实string类型排序
2. 异常要求是最后一个对象为ongoing，但是答案中例如Id为1021889的信息中只是status为ongoing，应该不符合题意
